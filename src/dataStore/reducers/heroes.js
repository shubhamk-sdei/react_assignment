import heroData from "../../data";

const initialState = heroData;

const heroesData = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default heroesData;
